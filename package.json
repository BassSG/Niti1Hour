{
  "name": "niti1hour-wash-dry-fold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ระบบรับฝากซัก-อบ-พับ แยกจากระบบรีดผ้าเดิมของนิติ รีดเรียบ",
  "scripts": {
    "test": "node --test",
    "check": "node --check worker/index.js && node --check packages/shared/business.js && node --check worker/index.test.js && node --test",
    "build:qr": "esbuild node_modules/qrcode/lib/browser.js --bundle --minify --format=iife --global-name=QRCode --outfile=apps/vendor/qrcode.min.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "promptpay-qr": "0.5.0",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
